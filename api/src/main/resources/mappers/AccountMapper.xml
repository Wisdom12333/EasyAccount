<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间应该是对应接口的包名+接口名 -->
<mapper namespace="com.shirj.api.dao.AccountDAO">

    <!--id应该是接口中的方法，结果类型如没有配置别名则应该使用全名称 -->
    <select id="getById" resultType="Account">
        SELECT *
        FROM T_ACCOUNT
        WHERE ACCOUNT_ID = #{id}
          AND REMOVE_TAG = '0';
    </select>

    <insert id="save"
            keyProperty="accountId"
            useGeneratedKeys="true"
            parameterType="Account">
        INSERT INTO T_ACCOUNT(ACCOUNT_NAME, USER_ID, TAG, BALANCE, REMARK, IS_TOTAL, CREATE_TIME, REMOVE_TAG)
        VALUES (#{accountName}, #{userId}, #{tag}, #{balance}, #{remark}, #{isTotal}, #{createTime}, #{removeTag});
    </insert>
</mapper>